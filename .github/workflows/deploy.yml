name: Deploy to Production

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch: # Permet de déclencher manuellement

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: 'npm'
          cache-dependency-path: nuxt/package-lock.json

      - name: Install dependencies
        working-directory: ./nuxt
        run: npm ci

      - name: Build Nuxt application
        working-directory: ./nuxt
        env:
          NUXT_PUBLIC_API_BASE: ${{ secrets.NUXT_PUBLIC_API_BASE || 'https://portfolio.vthiebaut.fr/api' }}
          NUXT_PUBLIC_EMAILJS_SERVICE_ID: ${{ secrets.NUXT_PUBLIC_EMAILJS_SERVICE_ID }}
          NUXT_PUBLIC_EMAILJS_TEMPLATE_ID: ${{ secrets.NUXT_PUBLIC_EMAILJS_TEMPLATE_ID }}
          NUXT_PUBLIC_EMAILJS_PUBLIC_KEY: ${{ secrets.NUXT_PUBLIC_EMAILJS_PUBLIC_KEY }}
        run: |
          echo "🔨 Build de l'application Nuxt..."
          npm run generate
          
          # Vérifier que le build a réussi
          if [ ! -d ".output/public" ]; then
            echo "❌ Erreur: Le build n'a pas généré .output/public"
            exit 1
          fi
          
          echo "✅ Build terminé"
          echo "📁 Contenu de .output/public:"
          ls -la .output/public | head -20

      - name: Create deployment archive
        run: |
          # Vérifier que les fichiers existent
          if [ ! -d "nuxt/.output/public" ]; then
            echo "❌ Erreur: nuxt/.output/public n'existe pas"
            ls -la nuxt/.output/ || echo "Le dossier .output n'existe pas"
            exit 1
          fi
          
          # Vérifier qu'il y a des fichiers
          if [ -z "$(ls -A nuxt/.output/public)" ]; then
            echo "❌ Erreur: nuxt/.output/public est vide"
            exit 1
          fi
          
          echo "📦 Création de l'archive..."
          # Créer l'archive depuis le répertoire de travail racine
          cd nuxt/.output/public
          tar -czf ${{ github.workspace }}/deploy-nuxt.tar.gz .
          cd ${{ github.workspace }}
          
          # Vérifier que l'archive a été créée et n'est pas vide
          if [ ! -f "deploy-nuxt.tar.gz" ] || [ ! -s "deploy-nuxt.tar.gz" ]; then
            echo "❌ Erreur: L'archive n'a pas été créée ou est vide"
            ls -la | grep tar || echo "Aucun fichier tar trouvé"
            exit 1
          fi
          
          echo "✅ Archive créée: $(du -h deploy-nuxt.tar.gz | cut -f1)"
          ls -lh deploy-nuxt.tar.gz
          pwd

      - name: Verify archive before deploy
        run: |
          if [ ! -f "deploy-nuxt.tar.gz" ]; then
            echo "❌ Erreur: deploy-nuxt.tar.gz n'existe pas"
            ls -la | grep tar || echo "Aucun fichier tar trouvé"
            exit 1
          fi
          
          if [ ! -s "deploy-nuxt.tar.gz" ]; then
            echo "❌ Erreur: deploy-nuxt.tar.gz est vide"
            exit 1
          fi
          
          echo "✅ Archive vérifiée: $(du -h deploy-nuxt.tar.gz | cut -f1)"
          ls -lh deploy-nuxt.tar.gz

      - name: Deploy to server
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          port: ${{ secrets.SERVER_PORT || 22 }}
          source: "deploy-nuxt.tar.gz"
          target: "/tmp/"
          strip_components: 0
          overwrite: true

      - name: Extract and deploy on server
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          port: ${{ secrets.SERVER_PORT || 22 }}
          script: |
            # Créer le dossier de déploiement s'il n'existe pas
            sudo mkdir -p /var/www/portfolio
            
            # Sauvegarder l'ancien build (optionnel, pour rollback)
            if [ -d "/var/www/portfolio" ] && [ "$(ls -A /var/www/portfolio 2>/dev/null)" ]; then
              sudo mv /var/www/portfolio /var/www/portfolio.backup.$(date +%Y%m%d_%H%M%S) || true
            fi
            
            # Extraire le nouveau build
            sudo mkdir -p /var/www/portfolio
            sudo tar -xzf /tmp/deploy-nuxt.tar.gz -C /var/www/portfolio
            
            # Définir les bonnes permissions
            sudo chown -R www-data:www-data /var/www/portfolio || sudo chown -R 1000:1000 /var/www/portfolio || true
            sudo chmod -R 755 /var/www/portfolio || true
            
            # Nettoyer
            rm -f /tmp/deploy-nuxt.tar.gz
            
            # Démarrer/redémarrage des containers Docker
            echo "🔄 Démarrage/redémarrage des containers Docker..."
            if command -v docker &> /dev/null; then
              cd /var/www/portfolio 2>/dev/null || {
                echo "❌ Impossible d'accéder à /var/www/portfolio"
                exit 1
              }
              
              if [ ! -f "docker-compose.prod.yml" ]; then
                echo "⚠️ docker-compose.prod.yml non trouvé dans /var/www/portfolio"
                echo "📁 Fichiers présents :"
                ls -la | head -10
                echo "⚠️ Démarrage manuel nécessaire"
                exit 0
              fi
              
              echo "📋 Fichier docker-compose.prod.yml trouvé"
              
              # Démarrer les containers s'ils ne sont pas déjà lancés
              echo "🚀 Démarrage des containers..."
              docker compose -f docker-compose.prod.yml up -d
              
              # Attendre un peu que les containers démarrent
              sleep 5
              
              # Vérifier que les containers tournent
              echo "🔍 Vérification de l'état des containers..."
              docker compose -f docker-compose.prod.yml ps
              
              # Redémarrer Caddy pour charger les nouveaux fichiers
              echo "🔄 Redémarrage de Caddy..."
              docker compose -f docker-compose.prod.yml restart caddy || docker compose -f docker-compose.prod.yml up -d caddy
              
              echo "✅ Containers Docker démarrés/redémarrés"
            else
              echo "⚠️ Docker non trouvé, démarrage manuel nécessaire"
            fi
            
            echo "✅ Déploiement terminé avec succès !"
            echo "📁 Fichiers déployés dans /var/www/portfolio"

      - name: Cleanup
        if: always()
        run: rm -f deploy-nuxt.tar.gz

